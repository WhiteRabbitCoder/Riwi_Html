# Nexcent Landing Page

Este proyecto es una réplica de la Landing Page de Nexcent, desarrollada como parte de la Prueba de Desempeño del Módulo 2 (HTML y CSS) en Riwi. El objetivo es crear una interfaz responsiva (To-Do), semántica y visualmente fiel al diseño proporcionado en Figma.

## 🚀 Despliegue (Live Demo)
You can see the project deployed here:
**https://nextcent-landingpage-riwi.netlify.app/**

## 🛠️ Technologies Used
* **HTML5:** Semantic structure.
* **CSS3:** Custom styles, Flexbox, CSS Grid, and CSS variables.
* **Google Fonts:** 'Inter' typeface.
* **FontAwesome:** Vector icons.

### Structure and Semantics
* Correct use of `<header>`, `<main>`, `<section>`, `<article>`, and `<footer>` tags.
* Correct heading hierarchy (`h1` - `h6`).
* Alternative text (`alt`) for all images.

### Visual Design (CSS)
* Implementation of a color palette using CSS variables (`:root`)
* Consistent typography loaded from Google Fonts.
* Hover effects and interactivity on buttons and cards.
* Design of overlapping cards in the Blog section using CSS positioning (To-Do).

### Responsive Design
The responsive design for this page will be planned as follows:
* **Desktop (≥ 1024px):** Full layout with 3-column grids.
* **Tablet (≥ 768px):** Adjusted spacing and reduced to 2 columns.
* **Mobile (≤ 480px):** Menus and layouts collapsed into a single column (Stacking).

## 📂 Project Structure

```text
/
├── assets/
│ ├── images/ # Graphic resources (Logos, illustrations)
│ └── styles/
│ └── style.css # Main stylesheet
├── index.html # Main structure
└── README.md # Documentation
```

### Autor
* Angelo Gaviria, 2025

Created purely for learning purposes.